{% extends 'base.html.twig' %}

{% block title %}Changement de mot de passe{% endblock %}

{% block body %}
    <h1>Changement de mot de passe</h1>

    {{ form_start(resetForm, {'attr': {'class': 'security'}}) }}
        {{ form_label(resetForm.plainPassword) }}
        {{ form_widget(resetForm.plainPassword) }}
        <button class="btn btn-primary">Changer</button>
    {{ form_end(resetForm) }}
{% endblock %}
